<h1>here are all cameras</h1>

<p>
  <span id="banner-typed-text"></span>
</p>

<!-- CARDS -->
<div id="card-countainer">
  <% @cameras.each do |camera| %>
    <div class="frame">
        <div class="namecamera">
         <%= camera.name %>
        </div>

         <div class="camera-description">
         <%= camera.address %>
         <%= camera.description %>
         </div>

         <div class="camera-price">
         <%= camera.price_per_day %>
        </div>
     </div>
</div>


<br>
